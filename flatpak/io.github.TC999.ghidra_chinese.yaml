app-id: io.github.TC999.ghidra_chinese
runtime: org.freedesktop.Sdk
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.openjdk21
command: ghidra
finish-args:
  - --share=network
  - --share=ipc
  - --socket=x11
  - --socket=pulseaudio
  - --device=dri
  - --allow=devel
  - --filesystem=home
  - --env=PATH=/usr/bin:/app/bin:/app/jdk/bin
  - --env=JAVA_HOME=/app/jdk
modules:
  - name: openjdk
    buildsystem: simple
    build-commands:
      - /usr/lib/sdk/openjdk21/installjdk.sh

  # from flathub: icoutils for extracting icon, reused as-is
  - name: icoutils
    cleanup:
      - '*'
    sources:
      - type: archive
        url: https://download.savannah.gnu.org/releases/icoutils/icoutils-0.32.3.tar.bz2
        sha256: 17abe02d043a253b68b47e3af69c9fc755b895db68fdc8811786125df564c6e0
      - type: patch
        path: icoutils-gcc15.patch

  # ---- 本地化：在两阶段 CI 中先 fetchDependencies 再 vendor 进来 ----
  # deps-vendor.tar.gz 包含 dependencies/ 和 flatRepo/，由 CI 阶段 1 (workflow) 生成
  # 解包到 build 目录后，gradle 就能无视这些目录直接 buildGhidra
  - name: ghidra
    buildsystem: simple
    build-commands:
      - export PATH="/usr/lib/sdk/openjdk21/jvm/openjdk-21/bin:$PATH" && export JAVA_HOME=/usr/lib/sdk/openjdk21/jvm/openjdk-21 &&
        ./gradlew -I gradle/support/fetchDependencies.gradle --offline --no-daemon --gradle-user-home=. ||
        true
      # use build script with vendored deps, no network needed
      - export PATH="/usr/lib/sdk/openjdk21/jvm/openjdk-21/bin:$PATH" && export JAVA_HOME=/usr/lib/sdk/openjdk21/jvm/openjdk-21
        && ./gradlew buildGhidra --offline --no-daemon
      - unzip -o build/dist/ghidra_*_*_linux_*.zip
      - icotool -x Ghidra/RuntimeScripts/Windows/support/ghidra.ico --index=8
      - install -d /app/lib
      - cp -r ghidra_*/ /app/lib/ghidra
      - install -Dm644 ghidra_8_256x256x32.png /app/share/icons/hicolor/256x256/apps/${FLATPAK_ID}.png
      - install -Dm644 ${FLATPAK_ID}.desktop /app/share/applications/${FLATPAK_ID}.desktop
      - install -Dm755 ghidra.sh /app/bin/ghidra
      - mv /app/lib/ghidra/support/launch.properties{,.orig}
      - ln -s /var/config/ghidra.properties /app/lib/ghidra/support/launch.properties
      - sed -i 's,bg,fg,' /app/lib/ghidra/ghidraRun
    sources:
      - type: git
        url: https://github.com/TC999/ghidra-chinese.git
        branch: chinese
      # 阶段1产物，由 CI workflow 生成后放在仓库根（或者以 local artifact 方式不在 git 里）
      - type: archive
        path: ../deps-vendor.tar.gz
        # 解压后覆盖 dependencies/ 和 flatRepo/
      - type: file
        path: io.github.TC999.ghidra_chinese.desktop
      - type: script
        dest-filename: ghidra.sh
        commands:
          - "[[ -f /var/config/ghidra.properties ]] || cp /app/lib/ghidra/support/launch.properties.orig /var/config/ghidra.properties"
          - exec /app/lib/ghidra/ghidraRun "$@"
